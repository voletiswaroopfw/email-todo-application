<div class="header-container">
  <div class={{activeMenuClass}} onclick={{action "toggleMenu"}}>
    <span></span>
    <span></span>
    <span></span>
  </div>
  {{#if showMenu}}
    {{hamburger-menu}}
  {{/if}}
</div>